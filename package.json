{
  "name": "agentry",
  "private": true,
  "version": "0.17.2",
  "description": "Dockerized Claude Code wrapper: REST API + UI on top of the CLI",
  "type": "module",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm -r --parallel --filter @agentry/api --filter @agentry/web dev",
    "dev:api": "pnpm --filter @agentry/api dev",
    "dev:web": "pnpm --filter @agentry/web dev",
    "build": "pnpm --filter @agentry/web build",
    "start": "pnpm --filter @agentry/api start",
    "typecheck": "pnpm -r typecheck",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "test": "pnpm --filter @agentry/shared --filter @agentry/core --filter @agentry/api --filter @agentry/web --filter @agentry/desktop test",
    "e2e": "node e2e/run.mjs",
    "media": "node scripts/record-media.mjs",
    "desktop:dev": "pnpm --filter @agentry/desktop dev",
    "desktop:dist": "pnpm --filter @agentry/desktop dist:linux"
  },
  "pnpm": {
    "//": "undici arrives with the Scalar docs viewer and every 7.x below 7.29.0 carries the twelve advisories pnpm audit reports, four of them high. Nothing here calls undici on purpose (the OIDC verifier goes through globalThis.fetch), but it ships in the image. Caret and not >=: undici 8 is a major the Scalar packages were not written against. CI runs pnpm audit --audit-level=high, so the next one fails a pull request instead of waiting for an audit.",
    "overrides": {
      "undici": "^7.29.0"
    }
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "axe-core": "^4.13.0",
    "gifenc": "^1.0.3",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2"
  }
}
